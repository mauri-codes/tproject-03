<% include _header %>
<h2>Usuarios Registrados en <%=device[0].devicename%></h2>
<div>
    <% device.forEach(function(dev) { %>
    <div class="panel panel-default <%= dev.username %>">
        <div class="panel-heading">
            <a href="/users/<%= dev.username %>">
                <%= dev.username %>
            </a>
            <%if(dev.accepted){%>
                <p class="deletereg btn btn-danger btn-xs pull-right">Eliminar Registro</p>
            <%} else {%>
                <p class="deletereg btn btn-warning btn-xs pull-right">Rechazar</p>
                <p class="acceptreg btn btn-success btn-xs pull-right">Aceptar</p>
            <%}%>
        </div>
    </div>
    <% }) %>
</div>
<% include _footer %>